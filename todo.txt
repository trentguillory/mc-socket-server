TODO:

- add sessions for safety, allow users to login based on Mojang account.
- custom logging to track everything server says. Need two versions. One for
  the admin to see all server logs, and another for all users to see chat
- plugin to change command prefix

ROUTES:
- Login - takes username, password. Returns Session object. User info, access, token
